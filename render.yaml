services:
  - type: web
    name: colabwize-backend
    env: node
    buildCommand: "npm ci --production=true"
    startCommand: "npm start"
    envVars:
      - key: NODE_ENV
        value: production