services:
  - type: web
    name: colabwize-backend
    env: node
    buildCommand: "npm install && npm list express"
    startCommand: "npm start"
    envVars:
      - key: NODE_ENV
        value: production